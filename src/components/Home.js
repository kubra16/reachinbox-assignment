import React from "react";
import EmailList from "./EmailList";

const Home = () => {
  return (
    <div style={{ height: "100vh", margin: 0 }}>
      <EmailList />
    </div>
  );
};

export default Home;
